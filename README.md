Simple genealogy database. Project in Dutch.

## Setup
Put the `www` folder on a server with PHP and adjust the MySQL configuration in `config.php`. The table structure is set up with `sql/init/familie.sql`.

